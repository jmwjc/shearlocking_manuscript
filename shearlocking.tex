\section{Optimal constraint ratio for shear locking}
\begin{equation}
\inf_{\boldsymbol q_h \in Q_h} \sup_{\boldsymbol u_h \in W_h \times \Phi_h}
\frac{\vert b(\boldsymbol u_h, \boldsymbol q_h) \vert}{\Vert \boldsymbol u_h \Vert_{W\times \Phi} \Vert \boldsymbol q_h \Vert_Q } \ge \beta_u
\end{equation}
\begin{equation}
\inf_{w_h\in W_h} \sup_{\boldsymbol q_h \in Q_h} \frac{\vert b_w(w_h,\boldsymbol q_h)\vert}{\Vert w_h\Vert_W \Vert \boldsymbol q_h \Vert_Q} \ge \beta_q
\end{equation}
\begin{equation}
\Vert w_h \Vert_W^2 = \int_\Omega \delta w_h kGh  w_h d\Omega
\end{equation}
\begin{equation}
\Vert \boldsymbol q_h \Vert_Q^2 = 
\int_\Omega \frac{\delta \boldsymbol q_h \cdot \boldsymbol q_h}{kGt} d\Omega
\end{equation}
\begin{equation}
\Vert \boldsymbol \varphi_h \Vert_\Phi^2 = 
\int_\Omega \delta \boldsymbol \kappa_h : \frac{t^3}{12} \boldsymbol D : \boldsymbol \kappa_h d\Omega
\end{equation}
\begin{equation}
\Vert \boldsymbol u_h \Vert_{W\times\Phi}^2 = 
\Vert w_h \Vert_W^2 + \Vert \boldsymbol \varphi_h \Vert_\Phi^2
\end{equation}

\subsection{Estimator for $\beta_q$}
\begin{equation}
\mathcal W \boldsymbol q
\end{equation}
\begin{equation}
b_w(w_h,\boldsymbol q_h) = (w_h, \mathcal W_h \boldsymbol q_h) = f_w(w_h)
\end{equation}
\begin{equation}
\inf_{Q'\subset Q_{n_q}\setminus\ker \mathcal W_h}
\sup_{\boldsymbol q_h \in Q'}
\frac{\Vert \mathcal W_h\boldsymbol q_h \Vert_W}{\Vert \boldsymbol q_h \Vert_Q}
\end{equation}
FIXME
\begin{equation}
Q_{n_q} = \{\boldsymbol q = q_\alpha \boldsymbol e_\alpha \in Q\vert \boldsymbol q\cdot \boldsymbol n = \bar q_n \; \textrm{on} \; \Gamma_q,\; q_\alpha \in \mathbb P_{n_q}\}
\end{equation}
\begin{equation}
n_u \le \langle \lfloor [n_q] \rfloor -1 \rangle
\end{equation}
\subsection{Estimator for $\beta_u$}
\begin{equation}
b(\boldsymbol u_h, \boldsymbol q_h) = b_\varphi(\boldsymbol \varphi_h, \boldsymbol q_h) + b_w(w_h,\boldsymbol q_h) = (\boldsymbol q_h, \mathcal Q_h \boldsymbol u_h) = f_q(\boldsymbol q_h)
\end{equation}
\begin{equation}
\inf_{U'\subset W_{n_w}\times \Phi_{n_\varphi}\setminus \ker \mathcal Q_h} \sup_{\boldsymbol u_h \in U'}
\frac{\Vert \mathcal Q_h \boldsymbol u_h\Vert_Q}{\Vert \boldsymbol u_h \Vert_{W\times \Phi}}
\end{equation}
\begin{equation}
\ker \mathcal Q = \left \{
\begin{pmatrix}
1 \\ 0 \\ 0
\end{pmatrix},
\begin{pmatrix}
x \\ 1 \\ 0
\end{pmatrix},
\begin{pmatrix}
y \\ 0 \\ 1
\end{pmatrix},
\begin{pmatrix}
x^2 \\ 2x \\ 0
\end{pmatrix},
\dots
\right \}
\end{equation}
\begin{equation}
\dim \ker \mathcal Q = \min (n_w,n_s)
\end{equation}
\begin{equation}
[n] = \frac{\sqrt{1+8n}-3}{2},\quad
\langle n \rangle = \frac{(n+1)(n+2)}{2}
\end{equation}
where $\langle [n] \rangle = n$
\begin{equation}
\langle \lfloor [n_\varphi] \rfloor+1 \rangle \le n_s \le \langle \lceil[n_\varphi]\rceil + 1 \rangle
\end{equation}
\begin{equation}
n_w^{\textrm{opt}} = \langle \lfloor [n_q] \rfloor -1 \rangle, \quad n_q^{\textrm{opt}} = n_\varphi
\end{equation}